name: CI Build
on:
  push:
    branches: [ "main" ]

permissions:
  contents: read
  packages: write
  id-token: write
  
jobs:
  build:
    uses: kfox1111/container-build-and-refresh-action/.github/workflows/containers.yml@main