name: Build
on:
  schedule:
    - cron: '0 8 * * *'

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

permissions:
  contents: read
  packages: write
  id-token: write
  
jobs:
  build:
    uses: kfox1111/container-build-and-refresh-action/.github/workflows/containers.yml@main
    with:
      repo: ghcr.io/kfox1111/testing-repo
